@using Main.Application.Dtos.Inventories.Index
@using Main.Application.Dtos.Items.Index
@using Main.Domain.enums.inventory
@using Microsoft.AspNetCore.Mvc.Localization
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResource> Localizer
@model IEnumerable<ItemDto>
@{
    var inventory = ViewBag.SelectedInventory as InventoryDetailsDto;
}

@if (inventory != null)
{
    <div class="d-flex justify-content-between align-items-center mb-4 mt-3">
            
            <a href="@Url.Action("Create", new { inventoryId = inventory.Id })" class="btn btn-success" style="color:white">
            <i class="fas fa-plus me-1"></i>@Localizer["Create New"]
            </a>
        <button type="button" id="deleteSelected" class="btn btn-danger "> <i class=" fas fa-trash me-1"></i>@Localizer["Delete Selected"]</button>
    </div>

    @if (Model?.Any() == true)
    {
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0"><i class="fas fa-table me-2"></i>Items (@Model.Count())</h5>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table mb-0" id="itemsTable">
                        <thead>
                            <tr>
                                <th>@Localizer["Select"]</th>
                                <th>@Localizer["Custom ID"]</th>
                                @foreach (var field in inventory.Fields.Where(f => f.IsVisibleInTable).OrderBy(f => f.OrderIndex))
                                {
                                    <th>@field.Name</th>
                                }
                                <th>@Localizer["Created By"]</th>
                                <th>@Localizer["Created At"]</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr data-item-id="@item.Id" class="clickable-row-item" style="cursor:pointer">
                                    <td>
                                        <input type="checkbox" name="selectedIds" value="@item.Id" class="rowCheckbox" />
                                    </td>
                                    <td><strong>@item.CustomId</strong></td>
                                    @foreach (var field in inventory.Fields.Where(f => f.IsVisibleInTable).OrderBy(f => f.OrderIndex))
                                    {
                                        var fieldValue = item.FieldValues?.FirstOrDefault(fv => fv.InventoryFieldId == field.Id);
                                        <td>
                                            @if (fieldValue != null)
                                            {
                                                @await Html.PartialAsync("~/Views/Inventories/Components/_FieldValueDisplay.cshtml", (field.FieldType, fieldValue))
                                            }
                                            else
                                            {
                                                <span class="text-muted">-</span>
                                            }
                                        </td>
                                    }
                                    <td>@item.CreatedById</td>
                                    <td>@item.CreatedAt.ToString("g")</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="card">
            <div class="card-body text-center py-5">
                <i class="fas fa-box-open fa-3x text-muted mb-3"></i>
                <h4>No items found</h4>
                <p class="text-muted">@Localizer["This inventory doesn't have any items yet"]</p>
                <a href="@Url.Action("Create", "Items", new { inventoryId = inventory.Id })" class="btn btn-success btn-lg">
                    <i class="fas fa-plus me-2"></i>@Localizer["Add First Item"]
                </a>
            </div>
        </div>
    }
}
else
{
    <div class="alert alert-warning">
        <h4><i class="fas fa-exclamation-triangle me-2"></i>@Localizer["Inventory not found"]</h4>
        <p>The selected inventory doesn't exist or you don't have access to it.</p>
        <a href="@Url.Action("Index", "Inventories")" class="btn btn-primary">@Localizer["Back to Inventories"]</a>
    </div>
}

@section Scripts {
   
}